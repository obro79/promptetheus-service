[build]
builder = "DOCKERFILE"
dockerfilePath = "Dockerfile"

[deploy]
healthcheckPath = "/health"
healthcheckTimeout = 100
restartPolicyType = "ON_FAILURE"
startCommand = "uvicorn promptetheus.server.app:create_app --factory --host 0.0.0.0 --port ${PORT}"
